/**
 * PostCSS Configuration
 *
 * Configures the PostCSS pipeline with:
 * - Tailwind CSS plugin
 * - Autoprefixer for browser compatibility
 */
export default {
  plugins: {
    '@tailwindcss/postcss': {},
    autoprefixer: {},
  },
}
